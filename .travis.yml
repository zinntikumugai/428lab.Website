language: node_js
node_js:
  - "10"

cache:
  directories:
    - "node_modules"

branches:
  only:
  - release
  - develop

script:
  - echo "Skipping tests"

before_deploy:
  - yarn install
  - yarn build
  - cd functions
  - yarn install
  - cd ..

deploy:
  # - provider: firebase
  #   skip-cleanup: true
  #   token: $FIREBASE_CI
  #   on:
  #     branch: release
  - provider: firebase
    # project: "develop"
    # skip-cleanup: true
    token:
      secure: $FIREBASE_CI
    on:
      branch: develop
    edge: true


# GitHub Pages
#  provider: pages
#  skip-cleanup: true
#  github-token: $GITHUB_ACCESS_TOKEN
#  target-branch: gh-pages
#  local-dir: dist
#  on:
#    branch: release
